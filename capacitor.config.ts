import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Bizorclass.app',
  appName: 'Bizorclass',
  webDir: 'dist/fuse',
  bundledWebRuntime: false
};

export default config;
